import { Config } from './types';

const configs: Config[] = [
  {
    platform: 'platform',
    network: 'network',
    displayName: 'Not Configured',
    url: 'conseil.server:port',
    apiKey: 'apikey',
    entities: []
  }
]

export default configs;
